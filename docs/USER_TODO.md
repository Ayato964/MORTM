# あなた（研究者）がやること — MORTM 論文プロジェクト

最終更新: 2026-07-12 / このファイルだけ見れば「あなたが手を動かすこと」が分かります。
（コード実装・データ処理・評価計測器は Claude 側で自走中。下の「Claude が自動で進めること」は放置でOK）

E1（A1 vs A2 の本学習＝論文の主結果）に着手するための残ゲートは **たった 3 つ**。すべて「人間の判断/作業」が必要な項目です。

---

## ✅ TODO-1【完了 2026-07-12】キー200曲の人手検証 — 一致率 74.3%(記入済)
**なぜ**: E3のキー分析の「正解」は music21 の自動ラベル。放置すると査読で「循環評価では？」と刺される。
人手一致率を測って「パイプラインラベルの信頼度」を論文で開示するため（§8.3 / E0-5）。

**やること**:
1. `docs/splits/key_verify_200.tsv` を開く（200曲、`pipeline_key(music21)` 列は記入済み）。
2. 各曲を聴く/理論判定し、**`human_key` 列**に自分の判断（例 `G major`, `E minor`）を記入。
3. **`match(y/n)` 列**に、music21ラベルと一致したか `y`/`n` を記入。
4. `Unknown`(7件)は判定不能でOK（空欄可）。
5. 可能なら**第三者1名**にも同じ検証を依頼（§8.3 推奨、必須ではない）。

**アウトプット**: 記入済み TSV。ここから一致率（例「87%」）を Claude が算出し論文 Data 節へ。
**所要**: 200曲なので数時間規模。分割してOK。

---

## ✅ TODO-2【完了 2026-07-12】事前登録の凍結 — commit cbf9705 / tag prereg-v1
**なぜ**: §7 の判定閾値（H1〜H5）は「最初のモデル評価を実行したら変更禁止」。E1 開始前に git で凍結する。
閾値の妥当性は既に確認済み（H2=Bavarian FIM、他=効果量/等価性の標準）→ あなたは「凍結してよい」の一言でOK。

**やること**:
- Claude に「**prereg-v1 で凍結して**」と言う（Claude が現作業を commit → `prereg-v1` タグ付け）。
- ※ commit はあなたの許可がないと Claude は実行しません。許可の一言だけください。

---

## ✅ TODO-3【完了 2026-07-13】TEST セット凍結 — TEST-SEQ + TEST-TASK(各1000窓) 構築・凍結(testset-v1)
**なぜ**: 評価用テストセットを固定（以後変更禁止）するため。
**やること**:
- Claude が **新split（曲単位98/1/1）で TEST-SEQ / TEST-TASK を構築中**。完成したら内容を提示するので、
  「**testset-v1 で凍結してよい**」と確認するだけ（構築はClaude、あなたは承認のみ）。

---

## ▶ 上記が済んだら（E1 着手）
TODO-1〜3 完了 → **E1（A1 vs A2 の本学習 + 方向別評価）に着手可能**。
E1 の学習実行（GPU）は Claude が起動・監視できます（前回SFTと同様）。あなたは結果を見るだけでも可。

---

## 🤖 Claude が自動で進めること（あなたは何もしなくてよい）
- 新split（train_songs.txt）での**学習索引の再構築**（A1/A2、test/valリーク防止） … 進行中
- **TEST-SEQ / TEST-TASK** の構築（test曲×5タスク×N=1000） … 進行中
- 評価ドライバ結線 / protocol_builder G形式 / E2 fullFT トレーナ … 実装済 or 予定
- 進捗は `docs/PROGRESS.md`、E0結果は `docs/E0_audit_report.md` に随時更新

---

## まとめ（これだけ覚えればOK）
1. **`key_verify_200.tsv` を埋める**（TODO-1、今すぐ）
2. 準備でき次第 **「prereg-v1で凍結して」** と言う（TODO-2）
3. TEST構築完了時に **「testset-v1で凍結してOK」** と確認（TODO-3）
→ その後 E1 本学習へ。
